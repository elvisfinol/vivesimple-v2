---
import ImageMod from "@/components/ImageMod.astro";
import Base from "@/layouts/Base.astro";
import { getListPage, getSinglePage } from "@/lib/contentParser.astro";
import { supportedLang } from "@/lib/utils/languageParser";
import { markdownify } from "@/lib/utils/textConverter";
import CallToAction from "@/partials/CallToAction.astro";
import Testimonial from "@/partials/Testimonial.astro";
import type { Feature } from "@/types";
import type { ContentEntryMap } from "astro:content";
import { FaCheck } from "react-icons/fa";
import Accordion from "@/shortcodes/Accordion"

export function getStaticPaths() {
  const paths = supportedLang.map((lang) => ({
    params: { lang: lang || undefined },
  }));
  return paths;
}

const { lang } = Astro.params;
const homepage = await getListPage("homepage", lang as keyof ContentEntryMap);
const { banner, features } = homepage[0].data;

const testimonial = await getSinglePage(
  "sections",
  lang as keyof ContentEntryMap,
  "testimonial"
);

const call_to_action = await getSinglePage(
  "sections",
  lang as keyof ContentEntryMap,
  "call-to-action"
);
---

<Base>

  
  
  
  <!-- Banner -->
  <section class="section pt-14">
    <div class="container">
      <div class="row justify-center">
        <div class="lg:col-7 md:col-9 mb-8 text-center">
          <h1
            set:html={markdownify(banner.title)}
            class="mb-4 text-h3 lg:text-h1"
          />
          <p set:html={markdownify(banner.content)} class="mb-8" />
          {
            banner.button.enable && (
              <a
                class="btn btn-primary"
                href={banner.button.link}
                target={
                  banner.button.link.startsWith("http") ? "_blank" : "_self"
                }
                rel="noopener"
              >
                {banner.button.label}
              </a>
            )
          }
        </div>
        {
          banner.image && (
            <div class="col-12">
              <ImageMod
                src={banner.image}
                height={380}
                width={1200}
                alt="banner"
                format="webp"
              />
            </div>
          )
        }
      </div>
    </div>
  </section>
  <!-- /Banner -->

  <section class="section py-12 bg-white">
    <div class="container mx-auto px-6 text-center">
      <h2 class="text-3xl font-bold text-gray-800 mb-8">Our Services</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        
        <!-- Service 1 -->
        <div class="p-6 border border-gray-200 rounded-lg hover:shadow-lg transition-shadow">
          <div class="mb-4">
            <img class="h-12 mx-auto" src="https://via.placeholder.com/50" alt="Service 1 Icon">
          </div>
          <h3 class="text-xl font-semibold text-gray-800">Service One</h3>
          <p class="mt-2 text-gray-600">Brief description of the service goes here. Keep it simple and concise.</p>
        </div>
        
        <!-- Service 2 -->
        <div class="p-6 border border-gray-200 rounded-lg hover:shadow-lg transition-shadow">
          <div class="mb-4">
            <img class="h-12 mx-auto" src="https://via.placeholder.com/50" alt="Service 2 Icon">
          </div>
          <h3 class="text-xl font-semibold text-gray-800">Service Two</h3>
          <p class="mt-2 text-gray-600">Brief description of the service goes here. Keep it simple and concise.</p>
        </div>
  
        <!-- Service 3 -->
        <div class="p-6 border border-gray-200 rounded-lg hover:shadow-lg transition-shadow">
          <div class="mb-4">
            <img class="h-12 mx-auto" src="https://via.placeholder.com/50" alt="Service 3 Icon">
          </div>
          <h3 class="text-xl font-semibold text-gray-800">Service Three</h3>
          <p class="mt-2 text-gray-600">Brief description of the service goes here. Keep it simple and concise.</p>
        </div>
  
        <!-- Service 4 -->
        <div class="p-6 border border-gray-200 rounded-lg hover:shadow-lg transition-shadow">
          <div class="mb-4">
            <img class="h-12 mx-auto" src="https://via.placeholder.com/50" alt="Service 4 Icon">
          </div>
          <h3 class="text-xl font-semibold text-gray-800">Service Four</h3>
          <p class="mt-2 text-gray-600">Brief description of the service goes here. Keep it simple and concise.</p>
        </div>
  
        <!-- Service 5 -->
        <div class="p-6 border border-gray-200 rounded-lg hover:shadow-lg transition-shadow">
          <div class="mb-4">
            <img class="h-12 mx-auto" src="https://via.placeholder.com/50" alt="Service 5 Icon">
          </div>
          <h3 class="text-xl font-semibold text-gray-800">Service Five</h3>
          <p class="mt-2 text-gray-600">Brief description of the service goes here. Keep it simple and concise.</p>
        </div>
  
        <!-- Service 6 -->
        <div class="p-6 border border-gray-200 rounded-lg hover:shadow-lg transition-shadow">
          <div class="mb-4">
            <img class="h-12 mx-auto" src="https://via.placeholder.com/50" alt="Service 6 Icon">
          </div>
          <h3 class="text-xl font-semibold text-gray-800">Service Six</h3>
          <p class="mt-2 text-gray-600">Brief description of the service goes here. Keep it simple and concise.</p>
        </div>
  
      </div>
    </div>
  </section>


  <!-- Features -->
  <!-- {
    features.map((feature: Feature, index: number) => (
      <section class={`section-sm ${index % 2 === 0 && "bg-gradient"}`}>
        <div class="container">
          <div class="row items-center justify-between">
            <div
              class={`mb:md-0 mb-6 md:col-5 ${index % 2 !== 0 && "md:order-2"}`}
            >
              <ImageMod
                src={feature.image}
                height={480}
                width={520}
                alt={feature.title}
                format="webp"
              />
            </div>
            <div class={`md:col-7 lg:col-6 ${index % 2 !== 0 && "md:order-1"}`}>
              <h2 set:html={markdownify(feature.title)} class="mb-4" />
              <p set:html={markdownify(feature.content)} class="mb-8 text-lg" />
              <ul>
                {feature.bulletpoints.map((bullet: string) => (
                  <li class="relative mb-4 pl-6">
                    <FaCheck className={"absolute left-0 top-1.5"} />
                    <span set:html={markdownify(bullet)} />
                  </li>
                ))}
              </ul>
              {feature.button.enable && (
                <a class="btn btn-primary mt-5" href={feature.button.link}>
                  {feature.button.label}
                </a>
              )}
            </div>
          </div>
        </div>
      </section>
    ))
  } -->
  <!-- /Features -->

  <section class="py-12 w-fit flex flex-col justify-center mx-auto">

    <div class="container mx-auto px-6 text-center">

      <h2 class="text-3xl font-bold text-gray-800 mb-8">Preguntas Frecuentes</h2>

  
  <Accordion client:load title="1. Should you use Negative margin?">
    Lorem ipsum dolor sit amet consectetur adipisicing elit. Ex, iste distinctio, eveniet eius soluta ratione cumque esse eum maiores alias, quam quia qui repudiandae iure vitae perspiciatis? Iusto, sed eos.
    </Accordion>

    <Accordion client:load title="2. Should you use Negative margin?">
      - This is a thing.
      - This is a thing.
      - This is a thing.
      - This is a thing.
      - This is a thing.
      
      </Accordion>


      <Accordion client:load title="3. Should you use Negative margin?">
        - This is a thing.
        - This is a thing.
        - This is a thing.
        - This is a thing.
        - This is a thing.
        </Accordion>

        <Accordion client:load title="4. Should you use Negative margin?">
          - This is a thing.
          - This is a thing.
          - This is a thing.
          - This is a thing.
          - This is a thing.
          </Accordion>

        </div>

    </section>


  <Testimonial testimonial={testimonial[0]} />
  <CallToAction call_to_action={call_to_action[0]} />

</Base>
